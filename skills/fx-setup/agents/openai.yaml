# generated by scripts/gen-command-skills from commands/fx-setup.md: edit there, not here
interface:
  display_name: "fx-setup"
  short_description: "Set up fx in this repository: reads the machine facts, asks what the repo cannot tell it, then writes .fx.json, repo.md, an optional CONTEXT.md and the directories the lanes expect"
policy:
  allow_implicit_invocation: false
