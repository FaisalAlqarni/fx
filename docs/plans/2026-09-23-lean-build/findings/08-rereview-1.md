# Task 08 re-review, fix round 1 (0abf558..9b29e0a)

Line numbers are at 9b29e0a (read with `git show 9b29e0a:<path>`).

### Finding verdicts (ADDRESSED | NOT ADDRESSED with file:line)

- **1. Lenses cannot write a findings file; fx-implement never gives one a path**: ADDRESSED under Ruling R. fx-review/SKILL.md:180-189 drops the five-line contract and the findings-file instruction and says lenses have no Write tool. fx-implement/SKILL.md:529-541 has the controller record the lens reply verbatim with a heredoc to `docs/plans/<slug>/findings/<NN>-lens-<name>.md` and says that path feeds the fix loop and the final review. fix-loop.md:30-31 and re-review-prompt.md:25-35 name the lens file as an input. See New breakage 1 for how lens items are then classed.
- **2. The fixer and re-reviewer are pointed at `### Issues` only**: ADDRESSED. fix-loop.md:29-39 and re-review-prompt.md:25-41 point at the findings file(s) as a whole with an open-items rule: spec ❌ under `### Spec Compliance`, Critical and Important under `### Issues`, confirmed ⚠️ through the new `Task <NN>: confirmed ⚠️: <exact text>` ledger line (SKILL.md:577-581), lens files, and for round 2+ the NOT ADDRESSED verdicts plus Critical/Important new breakage (fix-loop.md:44-47, re-review-prompt.md:36-38). `[PRIOR_FINDINGS_FILES]` is renamed consistently; no stale `[PRIOR_FINDINGS_FILE]` remains in the seven files.
- **3. Re-review reply and ledger lines drop out-of-scope and Minor breakage; New breakage has no severity; Open undefined**: ADDRESSED. re-review-prompt.md:150-153 adds a `Task <NN>: minor (deferred)` line per out-of-scope observation and per Minor new-breakage item; fix-loop.md:62-76 matches. `Open` is defined as NOT ADDRESSED plus new Critical/Important (re-review-prompt.md:164-165), and `New breakage` reports the highest severity (re-review-prompt.md:168-169, 187-188).
- **4. Nothing in the reply signals a plan-mandated finding**: ADDRESSED for the task reviewer. task-reviewer-prompt.md:298-299 appends `, plan-mandated: <n>` to `C/I/M`, task-reviewer-prompt.md:226 already labels such findings, and SKILL.md:399-400 lets the controller `grep -n plan-mandated`. The branch reviewer copy has a gap; see New breakage 2.
- **5. Reading rules forbid acting on BLOCKED specifics and reading Concerns**: ADDRESSED. SKILL.md:403-407 exempts BLOCKED and NEEDS_CONTEXT extra lines, matching implementer-prompt.md:353-354. SKILL.md:399-401 allows reading the report's Concerns section on a non-zero count, which squares with SKILL.md:477-478 ("Read them before proceeding") and the `Concerns` bullet in the report shape (implementer-prompt.md:343).
- **6. Ledger copy fails silently on the templates' own shapes; no count check**: NOT ADDRESSED for the re-review half. The shape fixes hold: all three templates now say `## Ledger lines`, "two `#` characters, not three", plain text, no bullet, no backticks (task-reviewer-prompt.md, reviewer-prompt.md, re-review-prompt.md:141-144), and the round line is unwrapped to one line (re-review-prompt.md:148). The reviewer count check works: the appended count must equal the Minor number in `C/I/M` (SKILL.md:411-414). The re-review check does not: SKILL.md:414-415, fix-loop.md:75-76 and re-review-prompt.md:156-157 compare against "1 plus the out-of-scope and Minor-breakage counts", but the re-review reply (re-review-prompt.md:161-169) carries neither count. `New breakage` gives only the highest severity. The controller cannot compute the expected number from the five lines, so a dropped out-of-scope or Minor line still vanishes without a signal, or the controller breaks its own reading rule to count them. Fix: carry the ledger-line count in the reply (for example `Fixed: <n>; ledger lines: <n>`), or have the re-review's round line state it.

### New breakage in the fix diff

1. **Important: "a lens has no severity split" is false, so lens Minors enter the loop and never reach the ledger.** fix-loop.md:34-35 and re-review-prompt.md:34-35 treat a lens file's entire reply as open because "a lens has no severity split to filter by". Every lens tags each finding `[Critical]`, `[Important]` or `[Minor]` in its Output section (agents/fx-lens-database.md:100-107, fx-lens-security.md:111-117, fx-lens-silent-failure.md:99-105, fx-lens-a11y.md:112-117, fx-lens-pipeline.md:169-180). As written, a lens Minor becomes an open item that the fixer must fix and the re-reviewer must verdict. That contradicts "Minor is never open" in the same paragraph (fix-loop.md:35-36, re-review-prompt.md:40) and "Minor findings never enter the loop" (fix-loop.md:12). It can spend fix rounds, and reach the round-5 breaker, on hardening notes. Lens Minors also get no `Task <NN>: minor (deferred)` line, since a lens reply has no `## Ledger lines`, so they never reach the final review's deferred list by that route. Fix: apply the same severity rule to a lens file ([Critical] and [Important] open, [Minor] not), and have the controller ledger lens Minors as deferred when it records the reply. Under Ruling R it already holds that reply in context.
2. **Minor: the branch reviewer is asked for a plan-mandated count it is never told how to label.** reviewer-prompt.md:203-204 appends `, plan-mandated: <n>`, but the branch template's body never defines a plan-mandated finding or the label (grep finds only the reply line; line 121-123 says only "say so"). task-reviewer-prompt.md:226 has the definition. The controller's `grep -n plan-mandated` on a branch findings file will usually find nothing. Fix: copy the task reviewer's one-sentence labeling rule into reviewer-prompt.md.
3. **Minor: re-review-prompt.md:26-27 says the controller quotes confirmed ⚠️ items "directly below", but the template has no slot for them** and the Placeholders list (re-review-prompt.md:172-185) names none. A controller filling the template has nowhere defined to put the quote. Fix: add a `[CONFIRMED_WARNINGS]` placeholder (the ledger lines, or "none"), or say where to append them.

### Out-of-scope observations

- fx-review/SKILL.md:186-189 sends a standalone fx-review (no fx-implement build) to fx-implement §3 for the lens-recording command, whose path needs `<slug>` and `<NN>`. A branch or ad hoc review outside a plan has neither. Minor.

Checks run: `node tests/review-bench/score.test.js` gave `score.test.js: ok`. `node tests/gates/return-contract.test.js` gave `return-contract: ok` (working tree, where task 11 is editing SKILL.md). I extracted the 9b29e0a task-reviewer-prompt.md body with fill-template.js's `extractPromptBody`, and it names exactly the eight KEYS tokens with no leftover after filling. Ruling A holds: no hunk touches the opening lines of the four templates, and the re-review hunk's lines 15-17 are context only.

### Verdict

**Fix round:** Findings remain open. Item 6 is NOT ADDRESSED: the re-review ledger count check compares against counts the reply does not carry. New Important breakage 1: lens Minors are treated as open because of a false "no severity split" claim, and they are never ledgered.

## Ledger lines

Task 08: fix round 1/5 (5 addressed, 2 open: re-review ledger count check needs counts the reply lacks; lens Minors treated as open and never ledgered, lenses do rate severity; commits 0abf558..9b29e0a)
Task 08: minor (deferred): reviewer-prompt.md asks for a plan-mandated count but never defines the plan-mandated label
Task 08: minor (deferred): re-review-prompt.md says confirmed warnings are quoted below but has no slot or placeholder for them
Task 08: minor (deferred): standalone fx-review lens recording points at an fx-implement path needing slug and task number
