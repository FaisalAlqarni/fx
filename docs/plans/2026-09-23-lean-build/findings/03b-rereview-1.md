### Finding verdicts (ADDRESSED | NOT ADDRESSED with file:line)

- findings/03b-review.md Critical 1 (search-case matches on the bare word "case"): ADDRESSED. `tests/review-bench/cases/search-case-sensitive/match:1` and `.../search-query-only/match:1` now read `case[- ]?(insensitiv|fold|sensitiv)|toLowerCase|toUpperCase|upper.?case|lower.?case|mixed.?case`. Verified directly with node: "test case", "edge case", and the review's own demonstrated false-catch sentence ("this test case only checks the happy path, add a failing lookup case too") all score false; "search is case-sensitive: it never calls toLowerCase" and "mixed case search should work" score true. `score.test.js` carries the same two assertions and passes.
- findings/03b-review.md Important 2 (unrealistic diff shape, whole app per case): ADDRESSED. `tests/review-bench/build-case.js` plus `tests/review-bench/good/` (`tests/review-bench/rows/01-review-bench.sh:31-45`) now builds the base commit as the good reference of every module except the case's own file(s), and the head commit as that base plus only the case's `files/`. Reproduced independently: built `path-no-check`'s base and head in a mktemp dir (removed after) and `git diff --stat` showed exactly `lib/store.js | 11 +++++++++++`, one file changed. `build-case.test.js` (27 checks, all ok) asserts this for all nine cases and that `traps.test.js` at head still scores only that case's own trap false, everything else including `control` true.
- findings/03b-review.md Important 3 (GLOBAL_CONSTRAINTS missing design.md's rules, Ruling P): ADDRESSED. `tests/review-bench/rows/01-review-bench.sh:32-45` now reads both `plan.md`'s `## Global Constraints` and `design.md`'s `## Storage and search rules`, verbatim, joined by the line `Storage and search rules (design.md):`. Ran the extraction directly: output is the three plan bullets, a blank line, the label, then the three design bullets (EBADNAME, ENOTE, case-insensitive search) verbatim, byte-for-byte matching `design.md`. On the label: it is not itself copied from either source, but it only names where the second block comes from and does not alter, summarize, or add to the rule text itself; a reviewer reading it is told exactly what it is, not told it is part of plan.md. Not misleading. If a byte-exact reproduction of design.md's own heading were wanted instead of this label, that is a cosmetic follow-up, not a defect.
- findings/03b-review.md Minor 5 (readme-binary match too generic): ADDRESSED. `tests/review-bench/cases/readme-binary/match:1` tightened to `no binary|not installed|command not found|node cli\.js instead|instead of.{0,20}node cli\.js|the example fails`. Verified: a benign usage example ("the usage example correctly runs node cli.js add and node cli.js show") scores false; "the example fails: it calls a `notes` binary that is never installed; it should use node cli.js instead" scores true. Matches `score.test.js`'s new assertions.
- findings/03b-lens-silent-failure.md item 1 (no audit trail for a miss): ADDRESSED. `FX_BENCH_KEEP` is plumbed through `run.sh` to the row (`tests/review-bench/rows/01-review-bench.sh:64-73`), which copies the filled prompt to `prompt.md` and, if present, the findings file to `findings.md` under `<dir>/<case>-<rep>/`, before the findings-file existence check, so a total miss still leaves the prompt to audit. Each copy is checked with `fail`, naming the destination.
- findings/03b-lens-silent-failure.md item 2 (leftover placeholder undetected): ADDRESSED. `tests/review-bench/fill-template.js` adds `findLeftoverPlaceholders`, scanning the template's own `[A-Z_]+` tokens for any still present after filling; the CLI exits 1 naming the token(s) if any remain. The row fills the prompt through this same CLI inside a command substitution with `|| fail "could not fill the reviewer template"`, so a leftover placeholder fails the row. Verified by the new `score.test.js` case (synthetic template with an unfilled `[NEW_PLACEHOLDER]`) and confirmed it is part of the passing `score.test.js: ok` run.

Not part of this fix round (deferred by the report, out of scope for this verification): findings/03b-lens-silent-failure.md item 3 (rev-parse unchecked) was folded in as a side effect: `tests/review-bench/rows/01-review-bench.sh` now has `BASE="$(...)" || fail ...` and `HEAD_SHA="$(...)" || fail ...`, confirmed in the diff. ADDRESSED.

### New breakage in the fix diff

None found. Ran `node tests/review-bench/score.test.js` (ok) and `node tests/review-bench/build-case.test.js` (27 checks, all ok) directly. `bash -n` clean on both `rows/01-review-bench.sh` and `run.sh`. The new `good/` directory sits under `tests/review-bench/`, not under `tests/review-bench/cases/`, so `run.sh`'s `CASE_DIRS=("$FX/tests/review-bench/cases"/*/)` glob does not pick it up as a spurious case. `scripts/check-all` gained the `review-bench-build-case.test.js` line as claimed.

### Out-of-scope observations

- The joining label ("Storage and search rules (design.md):") is implementer-authored, not verbatim from either source document. Judged above as not misleading, but if the owner wants GLOBAL_CONSTRAINTS to be reconstructible byte-for-byte from named headings alone, a comment or a heading-derived label would remove even the cosmetic gap.
- `score.js`'s bullet-line heuristic (`^\s*-\s+`) is unchanged this round and still undercounts a reviewer that numbers findings instead of dashing them; already disclosed by the implementer as not exercised since the live bench does not run in this task.

### Verdict

ADDRESSED (5/5 findings from findings/03b-review.md and 3/3 items from findings/03b-lens-silent-failure.md, including the deferred rev-parse minor). No new breakage found in the fix diff. Independent verification (regex tests, mktemp base/head build, both unit test suites) confirms the case diffs, the traps behavior, the tightened matches, the GLOBAL_CONSTRAINTS extraction, and the kept audit trail all work as claimed. Ready to proceed to bench baseline runs.
