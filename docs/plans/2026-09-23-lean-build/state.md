# fx ledger: plan: docs/plans/2026-09-23-lean-build/plan.md

Worktree: /development/fx/.worktrees/lean-build on lean-build (from main 649da01).
Baseline: setup OK; scripts/check-all ALL GREEN, 41 gates (2026-09-23).
Design read: docs/plans/2026-09-23-lean-build/design.md (amended after red-team).

## Pre-flight conflict scan

Pairs sharing a file or interface:

| Tasks | Shared | Produces / consumes | Found |
|---|---|---|---|
| 02, 03 | tests/conformance/lib/live.sh | 02 extracts the credential copy into scratch-home.sh; 03 adds FX_LIVE_TIMEOUT/MAX_TURNS | Different hunks; 03 blocked by 02. OK |
| 01, 02, 03, 08, 10, 11, 12 | scripts/check-all | each appends one run line | Serial by edges. OK |
| 05, 08, 11 | skills/fx-implement/SKILL.md | 05 Plan complete line; 08 reading rules and coverage-audit reply; 11 Parallel tasks | Different sections, serial by edges. OK |
| 01, 03, 08 | template opening lines | 01 and 03 classify transcripts by "You are implementing task", "You are reviewing", "You are re-reviewing"; 08 edits those templates | Ruling A |
| 01 | branch reviewer opening | reviewer-prompt.md:16 opens "You are a senior code reviewer", not "You are reviewing" | Ruling B |
| 03, 08 | implementer final reply | 03 implementer-heads takes the last SHA of the implementer's final message; 08 reply field Commits <base7>..<head7> | Last SHA is the head. OK |
| 03, 11 | ledger line form | 03 parses `Task NN: parallel with MM, branch <b>`; 11 writes `..., branch <b>, base <sha>, worktree <path>` | Prefix-compatible; parse by prefix. OK |
| 06, 12 | lib/preamble.test.js | 06 edits it; 12 maps hooks/ paths to it | Consistent. OK |
| 12, 13 | .fx.json isolated_test_execution | 12 records the concurrency check only; 13 sets the key on ship | Consistent after red-team #12. OK |
| 04, 07, 09, 13 | owner-run | fx-implement does not dispatch them | Stop and hand the owner the commands. OK |

Per-task self-consistency:

| Task | Found |
|---|---|
| 01 | Test fixtures match the definitions (5 calls, grep not counted, fixRounds 2). OK after Ruling B |
| 02 | Lane-coverage assertion needs all 12 model-facing prompts plus a none__ prompt; files list has them. OK |
| 03 | Self-test good/bad expectations checked by hand against traps. OK |
| 05 | Test uses repo() with a state string; supported by plan-state.test.js repo(). OK |
| 06 | Test uses INTRO, HARNESSES, render; INTRO exists at lib/preamble.test.js:73. Existing hook assertion updated per criteria. OK |
| 08 | Gate anchors: 'Lens briefs' in fx-review SKILL.md, 'one coverage audit' at fx-implement SKILL.md:590. OK |
| 10 | fx-setup/SKILL.md is generated from commands/fx-setup.md. OK after red-team #13 |
| 11 | Gate forbids "branch -D" and "rm -rf" text in the section. OK |
| 12 | GATES list built from tests/gates at run time. OK |

Ruling A: task 08 keeps the opening line of every template it edits ("You are implementing task [NN]", "You are reviewing one task's implementation", "You are re-reviewing", "You are a senior code reviewer"). Why: build-cost (01) and implementer-heads (03) classify subagent transcripts by those lines, and step measurements compare runs before and after 08. Cost if wrong: step 3's per-type numbers silently misfile, caught by task 08's review (this ruling is passed to its reviewer).
Ruling B: task 01's reviewer class also matches "You are a senior code reviewer" (reviewer-prompt.md:16, the branch reviewer). Why: otherwise branch reviews file as other. Task file amended. Cost if wrong: branch-review cost missing from the reviewer bucket, caught by task 01's review.
Ruling C: MVP tasks run serially (01, then 02, then 03). 01 and 02 have no edge between them, but .fx.json has no isolated_test_execution and the serial rule stands until task 11 exists. Cost if wrong: some minutes of wall-clock only.
Task 01: dispatched (implementer sonnet, BASE 81d6d93, agent af51774f008916221). Blocked on it: 02 serial per Ruling C.
Ruling D: the plugin version bump lands with task 01 (0.2.2 to 0.2.3, three manifests) instead of at the phase merge. Why: release-version.test.js fails check-all on the branch's first shipped-file change, and the plan says bump when it asks; later tasks on this branch compare against main and need no further bump. Cost if wrong: none beyond an earlier bump, caught by check-all at the exit gate.
Task 01: implementer DONE (4bd8448). Review dispatched: task reviewer (sonnet) + fx-lens-silent-failure (diff adds try/catch and || fallbacks on parse paths). Findings: findings/01-review.md.
Task 02: dispatched (implementer sonnet, BASE 4bd8448) while task 01 is in review. Ruling E: if task 01 needs a fix round, its fixer waits until task 02's implementer returns, so one writer is in the worktree at a time. Cost if wrong: a fixer and an implementer committing in one checkout, caught by the review packages' commit lists.
Task 01: silent-failure lens returned 2 Critical, 3 Important (findings/01-lens-silent-failure.md); cited lines verified at 29, 55, 58, 203-206, 214. Fix round waits for the task reviewer and for task 02's implementer (Ruling E).
Task 01: task review spec ✅, quality Approved, 0 C, 0 I, 4 M (findings/01-review.md). ⚠️ resolved by controller: commit 4bd8448 has no trailer and stages six paths by name (three are Ruling D's version bump); controllerActiveMs untested, deferred below.
Task 01: minor (deferred): controllerActiveMs has no assertion in build-cost.test.js.
Task 01: minor (deferred): median and p90 methods are not pinned by the task; confirm before comparing ctxMedian across runs.
Task 01: minor (deferred): ctxGrowthPerTask uses first and last completed in ledger order, not lowest and highest NN; add a one-line comment.
Task 01: fix round 1/5 queued (waits for task 02 implementer, Ruling E). Open, from the silent-failure lens: (1) key-less assistant record dropped silently; (2) unreadable subagents dir treated as none; (3) call usage frozen from a first record without usage; (4) malformed JSONL line crashes instead of exit 2; (5) unreadable subagent file skipped silently. The reviewer's JSON.parse minor is lens item 4. Intended remedy: each of 1, 2, 4, 5 exits 2 naming the file (and line where known); ENOENT on the subagents dir and an empty subagent file stay legitimate; 3 takes usage from the first record of the call that carries it.
Task 02: implementer DONE (1c63776). It reported check-all red from two causes outside the task; both were one cause, controller-owned: findings/01-review.md carried em dashes and an unbalanced parenthesis, which failed check-prose and the whole-repo walk inside check-prose-explicit-path.sh. Controller fixed the findings file prose; check-all ALL GREEN after.
Task 02: the implementer used git stash despite the dispatch; entry `task02-wip-check` on lean-build remains on the shared stack (content applied, drop blocked by a hook). Left in place; reported to the owner. Later dispatches repeat "never stash".
Task 01: fix round 1/5 dispatched (resumed original implementer, FIX_BASE 9480f52 is branch HEAD before the fix; the re-review package covers only the fix commits; findings by path). Task 02: review dispatched: task reviewer (sonnet) + fx-lens-security (credential copy and rm cleanup of a scratch home).
Task 02: security lens 1 Important, 3 Minor (findings/02-lens-security.md), citations verified. Minor 4 (OAuth refresh rotation) predates the plan (live.sh copies the credential the same way); owner informed, not in the loop.
Task 02: task review spec ✅, quality Approved, 0 C, 1 I, 2 M (findings/02-review.md).
Ruling F: task 02 review Important (git stash used against the dispatch; entry task02-wip-check still on the shared stack) does not enter the fix loop. Why: the violation is a past process event, not code; the entry is this branch's own, its content is applied and committed in 1c63776, and a repository hook blocks git stash drop. Cost if wrong: one stale stash entry on the shared stack, caught by the owner, who is told in the completion report.
Task 02: minor (deferred): the scratch-home setup block is duplicated between run-test.sh and run-reps.sh.
Task 02: minor (deferred): scripts/check-all whitespace alignment of the new run line.
Task 02: minor (deferred): log directories keep the default umask; set umask 077 as live.sh does (security lens 2).
Task 02: minor (deferred): scratch_home_claude trusts its directory argument; refuse a path under the real home (security lens 3).
Task 02: fix round 1/5 queued (waits for task 01's fixer, Ruling E). Open: security lens 1, run-test.sh and run-reps.sh run the session with no jail and export FX_REAL_HOME; the comment claims isolation the scripts do not give.
Task 01: fix round 1/5 implementer DONE (bcfcc86, 5 new tests, check-all green per report). Scoped re-review dispatched on 9480f52..bcfcc86. Task 02: fix round 1/5 dispatched (resumed original implementer, FIX_BASE bcfcc86); only writer in the worktree now.
Task 01: fix round 1/5 (5 addressed, 0 open; commits 9480f52..bcfcc86). Re-review findings/01-rereview-1.md.
Task 01: minor (deferred): the chmod 0o000 unreadable-file tests pass vacuously when run as root (re-review out-of-scope note).
Task 01: complete (commits 81d6d93..bcfcc86, review clean). Files: scripts/build-cost, scripts/build-cost.test.js, scripts/check-all, three manifests (Ruling D). Guarantee: node scripts/build-cost.test.js 11 cases; real transcript 61606dd5 reports 709 controller calls.
Blocked: task 03 needs task 02 (fix round 1 in flight); nothing else on the frontier.
Task 02: fix round 1/5 implementer DONE (df4c5af: claude call through tests/conformance/lib/jail.sh, FX_REAL_HOME no longer exported, new tests/lane-triggering/jail-isolation.test.sh RED then GREEN, check-all green per report). Scoped re-review dispatched on bcfcc86..df4c5af.
Ruling G: task 03 is dispatched while task 02's re-review runs. Why: task 03 consumes only tests/conformance/lib/scratch-home.sh and live.sh as landed in 1c63776 and df4c5af, and the re-review is read-only; if task 02 needs round 2, its fixer waits for task 03's implementer (same rule as Ruling E). Cost if wrong: task 03 builds on a task 02 shape a later round changes, caught by task 03's review reading the ledger.
Task 03: model most capable (opus). Reason: the largest task, and every later ship decision depends on the fixture measuring what it claims (fixture plan authoring, hidden trap tests, a jail-integrated live row).
Task 03: dispatched (implementer opus, BASE 4ff8360, agent ae8d44d77a90d2644).
Task 02: fix round 1/5 (1 addressed, 0 open; commits bcfcc86..df4c5af). Re-review findings/02-rereview-1.md.
Task 02: minor (deferred): jail-isolation.test.sh's dynamic "FX_REAL_HOME did not reach the claude process" check is vacuous, because the stand-in claude never prints its environment; the static grep beside it carries the proof.
Task 02: complete (commits 4bd8448..df4c5af, review clean). Files: tests/lane-triggering/{verdict.js,verdict.test.js,run-test.sh,run-reps.sh,run-all.sh,jail-isolation.test.sh,prompts/*,fixtures/*}, tests/conformance/lib/{scratch-home.sh,live.sh}, scripts/check-all. Guarantee: verdict.test.js, jail-isolation.test.sh, runner-isolation.test.sh green; live prompts owner-run in task 04. Ruling F stands (stash entry).
Task 03: implementer (opus) terminated by a weekly usage limit (HTTP 429, claude-opus-5-5) before committing. Uncommitted work on disk: seed repo, fixture plan, hidden tests, implementer-heads, row, run.sh, live.sh change; controller ran both unit tests (ok) and the no-"trap" grep (clean).
Ruling H: task 03 is finished by a fresh standard-tier (sonnet) implementer from the disk state, not by resuming the opus agent. Why: the opus limit resets 2026-09-28; the remaining work is audit, a README, check-all wiring, an environment check and the report. RED evidence is reconstructed by renaming modules under test, and the report must say so. Cost if wrong: an audit on sonnet misses a fixture-plan defect the opus author would have caught, caught by task 03's review (dispatched with this ruling) and by task 04's smoke run.
Owner note 2026-09-23: owner switched to an account with quota; Opus available again. Ruling H stands (the sonnet finisher is already running); later tasks use normal model routing, and the final branch review runs on the most capable tier.
Task 03: implementer DONE (74e7b06, finished under Ruling H; run.sh does not scrub the environment). Review dispatched: task reviewer (opus; reason: every later ship decision reads this fixture) + fx-lens-silent-failure (scorers map exceptions to false).
Task 03: silent-failure lens 1 Critical, 3 Important, 1 Minor (findings/03-lens-silent-failure.md); citations verified. Waiting for the opus task review before fix round 1.
Task 03: task review (opus) spec mostly met, quality needs fixes, 0 C, 4 I, 8 M (findings/03-review.md). Reviewer reproduced I1 on this build's own transcript: implementer-heads printed bcfcc86 and df4c5af where the original heads were 4bd8448 and 1c63776.
Ruling I: task 03 amended for review I1, I2, I3 and I4. I1: the implementer head is the last SHA of the first reply, because fx-implement resumes the same agent for fix rounds 1 to 3 and the final message is post-fix. I2 and I3: cli-wiring is judged by a SENTINEL stub of lib/search.js in a temp copy, so it measures wiring only and accepts any load form. I4: readme-example runs in a temp copy and is judged by the block's own output. Why: as specified, the by-review metric scores every catch as clean and one search defect counts twice; the defects were in the task's wording. Cost if wrong: the baseline measures the wrong thing, caught by the fix round's re-review and by task 04's smoke run.
Task 03: minor (deferred): a head SHA that predates lib/ or a stray hex word fails the whole run instead of scoring unknown (review M1).
Task 03: minor (deferred, owner task 11): step 4 takes the second worktree entry; once task 11 leaves task worktrees this can pick a task worktree; the fixture ledger's Worktree line names the build worktree (review M2). Task 11's review must check it.
Task 03: minor (deferred, owner task 11): mergeDefects fails the run when a ledger branch is gone, and the branch regex can keep a trailing backtick (review M3).
Task 03: minor (deferred): once merged to main, a jailed session could read hidden tests through $FX/.git; owner-run steps run from the build worktree, where the gitdir is hidden (review M4).
Task 03: minor (deferred, owner task 04): export-order likely reads clean every run and the self-test has no export-order false case; design.md states the readme answer outside task prose (review M5, M6). Task 04's sensitivity check owns trap difficulty.
Task 03: minor (deferred): model code calling process.exit ends traps.test.js with no JSON (review M7).
Task 03: minor (deferred): the report cites an optional step the task file does not have; it came from the finishing dispatch (review M8). Resolved by controller: the finishing dispatch named it.
Task 03: fix round 1/5 dispatched (resumed finishing implementer a818c06e322fad4ef, FIX_BASE 74e7b06 plus the controller's task-file amendment). Open: review I1 to I4 (Ruling I), lens 1 to 4.
Task 03: fix round 1/5 implementer DONE (f85ce20). Controller check (read-only, this session's transcript 6db8ac72): implementer-heads.js still prints {"01":"bcfcc86","02":"df4c5af"}, the post-fix heads. Cause, from the resumed agent file agent-af51774f008916221.jsonl: the fix-round message is a user record with isMeta true and string content "The coordinator sent a message while you were working: ...", and the fix skips every isMeta record as a skill expansion; skill expansions are isMeta records whose text starts "Base directory for this skill:" (records 16, 23, 72 there; the coordinator message is record 166). Review I1: NOT ADDRESSED. Fix round 2/5 dispatched for I1 only; the scoped re-review after it covers rounds 1 and 2 (74e7b06..head).
Task 03: fix round 2/5 implementer DONE (6ae266d). Controller verified on transcript 6db8ac72: implementer-heads prints {"01":"4bd8448","02":"1c63776"}, the original heads. Scoped re-review (opus; reason: these scorers decide every ship verdict) dispatched on 11fed10..6ae266d for review I1 to I4 and lens 1 to 4.
Task 03: fix rounds 1 and 2 of 5 (8 addressed, 0 open; commits 74e7b06..6ae266d). Re-review findings/03-rereview-1.md, with throwaway reproductions of the CLI and README cases.
Task 03: minor (deferred): readme-example relies on the README's `cli.js add` line; an unusual but correct README can still score false.
Task 03: minor (deferred): firstReply edge case with injected user messages.
Task 03: minor (deferred): the repo copy and SENTINEL stub write sit inside the trap's try, so a copy failure reads as a missed trap with only a stderr line.
Task 03: controller corrected the task file's self-test note for Ruling I (the buggy build's cli-wiring is true).
Task 03: complete (commits 4ff8360..6ae266d, review clean). Files: tests/fixture-build/ (repo, hidden, rows, run.sh, README.md), tests/conformance/lib/live.sh, scripts/check-all. Guarantee: traps.self-test.js and implementer-heads.test.js green; implementer-heads on transcript 6db8ac72 gives the original heads 4bd8448 and 1c63776; the live row is unrun until task 04.
Task 04: owner-run. Controller stopped and handed the owner the commands (smoke, then baseline, triggering, conformance, row 04 x10 on opencode). Resumes when the owner reports; controller then records measurements.md and runs the sensitivity check.
Owner authorized the controller to run task 04's live commands (2026-09-23). Smoke run started.
Task 04: live runs started by the controller: smoke (1 fixture build), row 04 x10 on opencode (local Qwen at 127.0.0.1:8899, log /tmp/fx-row04-opencode-baseline.log), lane-triggering run-all (log /tmp/fx-trig-baseline.log). Baseline fixture builds and the claude-code conformance run follow a clean smoke.
Task 04: lane-triggering baseline: 17 passed, 0 failed (/tmp/fx-lane-triggering/460150/..., real sessions: research stream 379 KB, 72 assistant turns, invoked fx:research; none__git-question rerun invoked no lane).
Ruling J: run-all.sh counts a run-test.sh [SKIP] (no CLI, no credential) as PASS, so a missing credential would read as a clean 17/17. Until fixed, every triggering result recorded in measurements.md is backed by a check that a stream log with assistant turns exists for that run. Cost if wrong: a fake pass at step 2, 3 or 4, caught by this check. Flagged for the final review as a defect in the triggering instrument (task 02's area).
Task 04: row 04 on opencode baseline 10/10 PASS (/tmp/fx-row04-opencode-baseline.log).
Task 04: smoke-1 PASS end to end in 47m: caught at end 6/6; byReview clean for all six (every implementer got every trap right first time); controller 55 calls, 5.3M tokens, ctx median 95.8K, p90 148.8K, growth 7.6K per task, fixRounds 1; total tokens 25.0M; subagents implementer 7, reviewer 7, lens 1, coverage 1, other 13. Anomalies: tasksCompleted 7 for a six-task plan; 13 subagents unclassified; transcripts were deleted with the scratch dir, so neither can be diagnosed from this run.
Ruling K: the task 04 sensitivity check is applied to the smoke run, not after three baseline builds. Why: 0 of 6 traps were red at an implementer head, so review had nothing to catch; three more builds (about 2.5 hours, about 75M tokens) would confirm a fixture that cannot show a review loss. Task 03 reopened (fix round 3/5): path-escape, missing-note-error and search-case lose their rule from task prose and keep a weak hint there; the exact rule and error codes move into the fixture design's body (not its Global Constraints), where the coverage audit and branch review read it; the row gains FX_FIXTURE_KEEP to copy a run's transcripts out before the scratch dir is deleted. Then smoke-b. Cost if wrong: traps become so hard that every run misses them at the end, caught by smoke-b's caught-at-end count.
Task 03: reopened, reason: fixture too easy (smoke-1 byReview all clean).
Task 03: fix round 3/5 implementer DONE (602d43a). Scoped re-review dispatched on 5fada95..602d43a; smoke-b started in parallel with FX_FIXTURE_KEEP=/tmp/fx-keep (discarded if the re-review finds a defect). Controller noted Ruling K in task 03's trap table.
Task 03: fix round 3/5 (5 addressed, 0 open; commits 5fada95..602d43a). Re-review findings/03-rereview-3.md. Task 03: complete again (commits 4ff8360..602d43a, review clean); smoke-b in flight decides whether the fixture is sensitive enough.
Task 04: smoke-b PASS in 56m: caught at end 6/6, byReview clean for all six again; controller 61 calls, 6.4M tokens, ctx median 104.6K; total 29.0M; subagents implementer 7, reviewer 7, lens 6, re-review 2, coverage 2, other 5. Transcripts kept in /tmp/fx-keep/smoke-b-1.
Diagnosis from the kept transcripts: task "07" is a coverage-gaps task the pre-review coverage audit added (explains tasksCompleted 7). The task 01 implementer (agent-ac1d1fec53fa2be1e) cites design.md 8 times and wrote err.code = 'EBADNAME' itself: implementers read the design, so moving a rule from task prose into the design hides nothing from them. Any document a reviewer can read, the implementer can read.
Ruling L: the fixture's traps stay stated, and the hidden tests gain edge-case variants of each stated rule that a naive implementation misses: path-escape adds an absolute path, `a/../../x`, and a bare `..`; missing-note-error adds that a saved empty note loads as '' and does not throw; search-case adds a mixed-case match inside a word. Why: review's real value is catching a wrong implementation of a known rule, not supplying a hidden one; a fixture where nothing is red at an implementer head cannot show a review loss. Cost if wrong: variants so hard that review never fixes them (caught at end drops), caught by smoke-c; or still too easy, caught by smoke-c's byReview. Task 03 fix round 4/5.
Task 03: fix round 4/5 implementer DONE (f334d73). Scoped re-review on 7e5098b..f334d73 and smoke-c (FX_FIXTURE_KEEP=/tmp/fx-keep) started in parallel.
Task 03: fix round 4/5 (5 addressed, 0 open; commits 7e5098b..f334d73). Re-review findings/03-rereview-4.md.
Ruling M: the re-review's item 4 caveat is a real fairness defect: a store using path.join with a startsWith check nests an absolute name inside NOTES_DIR, never writes outside it, and still scores path-escape false because it does not throw EBADNAME; the fixture design only requires rejecting names that would resolve outside. It goes into fix round 5 (the last), held until smoke-c reports so that one round also covers anything smoke-c shows: the absolute-path case scores safety (nothing read or written outside NOTES_DIR), not the error code. Cost if wrong: a correct build scored as a review miss, inflating "missed" and blocking a good step; smoke-c's result is read with this known bias.
Task 04: smoke-c PASS in 42m: caught at end 6/6; byReview clean for all six again (edge-case variants included); controller 54 calls, 5.6M tokens, ctx median 102.6K; total 17.9M; fixRounds 0. Three smoke builds, 18 trap instances, none red at an implementer head.
Owner decision (2026-09-23, asked by the controller): quality for steps 2 to 4 is end quality plus a review bench. Design, plan and tasks 04, 07, 09, 13 amended: the ship rule reads caught at end, the bench (per planted defect, control false positives), row 04 and triggering; byReview is recorded, not gating. New task 03b (review bench) blocks task 04.
Task 03: fix round 5/5 dispatched for Ruling M (absolute-path case scores safety, not the error code).
Task 03: fix round 5/5 implementer DONE (2ab1c3f). Scoped re-review on 5d4863c..2ab1c3f. Task 03b dispatched in parallel (implementer sonnet, BASE 2ab1c3f); Ruling E pattern: task 03 has no rounds left, so its re-review ends in a clean verdict or adjudication, never another writer.
Task 03: fix round 5/5 (Ruling M addressed; 1 open: a store that sends an absolute name to a third location outside NOTES_DIR, neither the named target nor inside, scores path-escape true; reproduced by the re-reviewer). Findings findings/03-rereview-5.md.
Ruling N (at the cap, parked): the open path-escape hole stays. Why: the leak it misses needs a store that invents a third outside path for absolute names; the realistic leak, writing or reading at the absolute target itself, is still scored false, and `a/../../x` and `..` still require EBADNAME. Closing it needs a scan of the whole temp tree around NOTES_DIR, a sixth round the cap does not allow. Cost if wrong: an exotic leaky store scores path-escape true, overstating caught-at-end for that trap; caught by the final branch review, which is pointed at this parked line.
Task 03: complete (commits 4ff8360..2ab1c3f, 1 parked, Ruling N). Five fix rounds. Guarantee: traps.self-test.js and implementer-heads.test.js green; implementer-heads gives the original heads on transcript 6db8ac72; smoke-1, smoke-b, smoke-c each ran end to end.
Ruling O (owner delegated: "do what you see fit and quicker"): each measurement runs 2 fixture builds in parallel (labels <step>-a and <step>-b), not 3 in series; the review bench keeps 3 reps. Why: caught at end was 6/6 in all three smoke builds, so the fixture's extra run mainly narrows cost medians, and quality now rests on the bench; parallel builds cut a measurement's wall clock from about 2.5h to about 1h. Design, plan and tasks 04, 07, 09, 13 amended. Cost if wrong: noisier cost medians, so a small real saving can read as none; caught when a step's cost verdict is within noise, in which case a third build is run for that step only.
Task 03b: implementer DONE (879e438; added fill-template.js helper, reported). Review dispatched: task reviewer (sonnet) + fx-lens-silent-failure.
Task 04: baseline fixture builds baseline-a and baseline-b started in parallel at 879e438 (fixture complete; they do not depend on task 03b). Bench baseline follows task 03b's review.
Task 03b: silent-failure lens 2 Important, 1 Minor (findings/03b-lens-silent-failure.md), claims verified.
Task 03b: task review 1 C, 2 I, 2 M, needs fixes (findings/03b-review.md). C1: the case-sensitivity cases' match is the bare word "case" and scores "test case" as a catch (reviewer demonstrated). I2: each case diff is the whole app on an empty seed, not one task's change on the earlier tasks' work. I3: [GLOBAL_CONSTRAINTS] reads only plan.md's section, not the fixture design's "Storage and search rules".
Ruling P: the bench's [GLOBAL_CONSTRAINTS] also carries the fixture design's "Storage and search rules" section verbatim. Why: fx-implement fills that block from "plan.md or design.md", and a real controller copies the binding rule a task must meet; without it the bench measures a harder review than fx runs. Cost if wrong: bench catch rates read higher than a review with only plan constraints, caught by comparing against the fixture builds' own reviewer dispatch prompts in /tmp/fx-keep.
Task 03b: fix round 1/5 dispatched (resumed implementer, FIX_BASE 879e438). Open: review C1, I2, I3 (Ruling P); lens 1 (keep findings file and filled prompt), lens 2 (fail on a leftover placeholder). Review M5 (readme match) folded in since the match files are being edited.
Task 03b: fix round 1/5 implementer DONE (e32118c). Scoped re-review on 879e438..e32118c; bench baseline (3 reps, FX_BENCH_KEEP=/tmp/fx-bench-keep) started in parallel, discarded if the re-review finds a defect.
Task 03b: fix round 1/5 (7 addressed, 0 open; commits 879e438..e32118c). Re-review findings/03b-rereview-1.md; reproduction confirms single-file case diffs.
Task 03b: minor (deferred): score.js counts only single-line dash bullets; numbered or wrapped findings undercount (review M4).
Task 03b: minor (deferred): GLOBAL_CONSTRAINTS joins the two sources under a label the implementer wrote.
Task 03b: complete (commits a9df81b..e32118c, review clean). The bench baseline running now is valid (started at e32118c).
Ruling Q (owner asked to speed up, 2026-09-23): (1) the remaining code tasks are built now, while the baseline runs, in two sibling worktrees branched from b7d1091, so the baseline's --plugin-dir tree is never edited: lane A /development/fx/.worktrees/lean-a (branch lean-a) takes 05, 08, 11, which share skills/fx-implement/SKILL.md; lane B /development/fx/.worktrees/lean-b (branch lean-b) takes 06, 10, 12, which touch disjoint files. One implementer per lane at a time; fx's own tests use per-process temp paths. Both lanes merge into lean-build after the baseline finishes. (2) Steps 2, 3 and 4 are measured once, together, against the baseline (tasks 07 and 09 fold into 13); if quality drops, intermediate commits are measured to find the step. Cost if wrong: a regression costs a bisect run to attribute, and lane conflicts cost a merge, caught at the merge by check-all and by the combined measurement.
Task 05: dispatched in lane A (sonnet, BASE b7d1091, worktree lean-a). Task 06: dispatched in lane B (sonnet, BASE b7d1091, worktree lean-b).
Task 05: implementer DONE (da91b73, lane A). Review dispatched (reviewer sonnet + silent-failure lens: plan-state reads state.md under try/catch). Task 08 dispatched in lane A (BASE da91b73); a 05 fixer, if needed, waits for 08 (Ruling E pattern).
Task 06: implementer DONE (ef5206f, lane B; Codex hook also passes the subagent flag). Review dispatched (reviewer sonnet; no lens trigger). Task 10 dispatched in lane B (BASE ef5206f).
Task 05: task review Approved, 0 C, 0 I, 1 M (findings/05-review.md). Task 05: minor (deferred): an unreadable state.md now renders as no ledger, changing the message and top-3 bucketing; untested. Waiting on the silent-failure lens before completing task 05.
Task 05: silent-failure lens 1 Critical, 1 Important, 1 Minor (findings/05-lens-silent-failure.md), verified. Fix round 1/5 queued for task 05: hasState from existence, not readability, plus the test gaps; waits for task 08's implementer in lane A.
Task 06: task review Approved, 0 C, 0 I, 5 M (findings/06-review.md; minors deferred to the final review). Task 06: complete (commits b7d1091..ef5206f on lean-b, review clean).
Task 10: implementer DONE (a076044, lane B). Review dispatched (reviewer sonnet). Task 12 dispatched in lane B (BASE a076044).
Task 10: task review Approved, 0 C, 0 I, 2 M (findings/10-review.md; deferred). Task 10: complete (commits ef5206f..a076044 on lean-b, review clean).
Task 08: implementer DONE (940b6f8, lane A; added [PRIOR_FINDINGS_FILE] and [ROUND] to re-review-prompt.md; flagged that fx-implement §3 lens dispatch names no findings path, which is in task 08's own file list). Review dispatched (opus; reason: judging whether the fix loop still gets what it needs). Task 05 fix round 1/5 dispatched (resumed implementer, FIX_BASE 940b6f8).
Task 12: implementer DONE (10e90fd, lane B). Review dispatched (reviewer sonnet).
Task 08: task review (opus) spec ✅, quality needs fixes, 0 C, 6 I, 4 M (findings/08-review.md). Walking the fix loop end to end broke at: (1) lenses cannot write a findings file; (2) the fixer and re-reviewer are pointed only at ### Issues, which misses spec ❌, confirmed ⚠️, lens findings and every round after the first; (3) re-review lines drop out-of-scope and Minor breakage, and New breakage has no severity; (4) the reply does not flag a plan-mandated finding; (5) the reading rules forbid the BLOCKED and concern detail §2 needs; (6) the ledger copy fails silently on the heading and line shapes the templates themselves show.
Ruling R: lenses stay read-only (agents/fx-lens-*.md grant Read, Grep, Glob; Codex role generation keys on that) and return their full findings as their reply, as before task 08; the five-line contract and the findings-file instruction are removed from the lens brief, and fx-implement §3 records a lens's reply to a findings file itself by writing the reply text with a shell heredoc, without reasoning over it. Why: task 08's text asked read-only agents to write a file, which they cannot; lens replies are short (under 40 lines in this build). Cost if wrong: a lens reply enters controller context in full, a small cost, caught by build-cost's controller numbers at the combined measurement. The task file is amended to match.
Task 08: fix round 1/5 queued for review items 1 to 6 under Ruling R; waits for task 05's fixer in lane A (one writer).
Task 05: fix round 1/5 implementer DONE (0abf558). Scoped re-review dispatched. Task 08: fix round 1/5 dispatched (resumed implementer, FIX_BASE 0abf558).
Task 05: fix round 1/5 (3 addressed, 0 open; commits 940b6f8..0abf558). Re-review findings/05-rereview-1.md. Task 05: complete (commits b7d1091..0abf558 on lean-a, review clean; da91b73 and 0abf558).
Task 12: task review 2 C, 0 I, 2 M, needs fixes (findings/12-review.md): no check-all fallback for unmapped paths; .codex-plugin and .agents paths routed to a check that never reads them. Fix round 1/5 dispatched (resumed implementer, lane B, FIX_BASE 10e90fd).
Task 12: fix round 1/5 implementer DONE (a6d4ae6). Controller dry-run check: lib/preamble.js and lib/plan-state.js do not get the hook gates the task names (only their sibling test); passed to the scoped re-review as a named finding.
Task 12: fix round 1/5 (2 addressed, 1 open: lib/preamble.js and lib/plan-state.js miss the hook gates, predates the round; commits 10e90fd..a6d4ae6). Fix round 2/5 dispatched.
Task 08: fix round 1/5 implementer DONE (9b29e0a; replied in the new five-line form). Scoped re-review (opus) on 0abf558..9b29e0a. Task 11 dispatched in lane A (BASE 9b29e0a); a task 08 round 2, if needed, waits for 11.
Task 04: bench baseline stopped at 20 of 27 reps by the controller. Live data showed a bench flaw: case diffs carry no test file, so the reviewer rightly reports "no test written" as Critical; control scored a false positive in 2 of 3 reps (control-3 findings: Critical "No test file was written"). Partial results discarded.
Ruling S: task 03b reopened (fix round 2/5): every case carries its fixture task's test file as the fixture plan writes it out, and the report placeholder states the tests ran with their output; the bench baseline is rerun after the fix. Why: a real fx task diff always has its test; without it the control cannot measure false positives and every case carries a distraction. Cost if wrong: about 80 minutes of bench rerun, caught by the rerun's control scoring.
Task 04: fixture baseline done: baseline-a PASS (33m, 19.3M tokens, 41 controller calls, end 6/6), baseline-b PASS (55m, 22.8M tokens, 61 controller calls, end 6/6). The wrapper's exit 1 came from its trailing tail, not a build.
Task 08: fix round 1/5 (5 addressed, 1 open: item 6's re-review count check needs counts the re-review reply does not carry; new Important breakage: fix-loop.md:34 and re-review-prompt.md:34 treat lens findings as unsplit, so lens Minors enter the loop; commits 0abf558..9b29e0a). Re-review findings/08-rereview-1.md.
Task 08: minor (deferred): reviewer-prompt.md:204 asks for a plan-mandated count it never defines.
Task 08: minor (deferred): re-review-prompt.md:26 has no slot for quoted confirmed warning items.
Task 08: fix round 2/5 queued; waits for task 11's implementer in lane A.
Task 11: implementer DONE (7c254dc, lane A). Review dispatched (opus; reason: parallel failure paths and resume). Task 08: fix round 2/5 dispatched (resumed implementer, FIX_BASE 7c254dc).
Task 12: fix rounds 1 and 2 of 5 (3 addressed, 0 open; commits 10e90fd..1e93b64). Re-reviews findings/12-rereview-1.md, 12-rereview-2.md. The fixer resolved a conflict between the task's example test and its Interfaces in favour of the Interfaces (more gates run); controller agrees: the design and Interfaces bind.
Task 12: minor (deferred): a marketplace.json path routing note and an untested .test.sh plus skills overlap (re-review out-of-scope items).
Task 12: complete (commits a076044..1e93b64 on lean-b, review clean). Concurrency check: concurrent check-all: pass. Lane B is done.
Task 11: task review (opus) needs fixes, 0 C, 3 I, 5 M (findings/11-review.md). I1: the fixture row's step 4 takes the second git worktree list entry, and git sorts entries by path, so a leftover task worktree such as .worktrees/01-store sorts before the build worktree (reviewer checked git 2.43; the report's creation-order reasoning was wrong); this is the item deferred from task 03 with task 11 as owner. I2: resume has no fallback to serial for a missing recorded worktree, an existing back-to-serial line, or an implementer that died mid-task. I3: the file check runs only when the implementer first returns, so fix rounds can touch files outside both lists and merge without a fresh check.
Task 11: fix round 1/5 queued for I1 to I3 (I1 edits tests/fixture-build/rows/01-fixture-build.sh step 4 in lane A); waits for task 08's round 2 in lane A.
Task 08: fix round 2/5 implementer DONE (77ac5fa). Scoped re-review (opus) on 7c254dc..77ac5fa. Task 11: fix round 1/5 dispatched (resumed implementer, FIX_BASE 77ac5fa).
Task 03b: fix round 2/5 implementer DONE (8737a9c, Ruling S: cases carry their test files, report states tests ran). Controller fixed dashes in reviewer-written findings files (05, 06, 12 reviews) that failed check-prose. Fix round 3/5 dispatched: the bench's good README lacks the ## Usage heading its fixture test needs.
Task 08: fix round 2/5 (2 addressed, 0 open; commits 7c254dc..77ac5fa). Re-review findings/08-rereview-2.md.
Task 08: minor (deferred): fix-loop.md:35-36 says "below" for a step that lives in SKILL.md section 3; SKILL.md:606-608 argues against a sentence round 2 deleted; plus 2 out-of-scope minors in 08-rereview-2.md.
Task 08: complete (commits da91b73..77ac5fa on lean-a: 940b6f8, 9b29e0a, 77ac5fa; review clean).
Task 03b: fix round 3/5 implementer DONE (470cf25). Scoped re-review over rounds 2 and 3 (tests/review-bench changes b7d1091..470cf25); bench baseline rerun started in parallel (FX_BENCH_KEEP=/tmp/fx-bench-keep2).
Task 03b: fix rounds 2 and 3 of 5 (2 addressed, 0 open; commits 8737a9c, 470cf25). Re-review findings/03b-rereview-2.md. Task 03b: complete again (commits a9df81b..470cf25, review clean); the bench baseline now running (started at 470cf25) is valid.
Task 04: Claude Code conformance baseline 18 pass, 0 fail, 0 gap (/tmp/fx-conf-cc-baseline.log), row 04 PASS.
Task 11: fix round 1/5 implementer DONE (528450f; new tests/fixture-build/hidden/pick-build-worktree.test.sh). Scoped re-review (opus) on 77ac5fa..528450f.
Lanes merged: lean-a into lean-b at 643da94, no conflicts; scripts/check-all ALL GREEN, 53 gates (baseline had 41). lean-build is fast-forwarded to it after the bench baseline finishes (the bench runs from lean-build's tree).
Combined measurement (Ruling Q) started early from lean-b at 643da94 (all new code): fixture builds combined-a and combined-b with FX_FIXTURE_PARALLEL=1 in parallel, bench 3 reps, lane-triggering, claude-code conformance. Task 11's re-review is still open; if it changes code, the affected measurements are rerun. Row 04 on opencode follows when lean-b's tree is final.
Task 11: fix round 1/5 (3 addressed, 0 open; commits 77ac5fa..528450f). Re-review findings/11-rereview-1.md, reproduced with a task worktree sorting first.
Task 11: minor (deferred): the picker needs the ledger's worktree path absolute and exact; otherwise it fails as ambiguous, never picks wrong.
Task 11: minor (deferred): resume does not name the case of an implementer that left commits but no report.
Task 11: complete (commits 9b29e0a..528450f on lean-a, review clean). All code tasks complete: 01, 02, 03, 03b, 05, 06, 08, 10, 11, 12. lean-b at 643da94 is final for the combined measurement.
Combined: lane-triggering 17 passed, 0 failed (Ruling J check: stream logs with assistant turns present, 67 in the latest); claude-code conformance 18 pass, 0 fail, 0 gap.
Combined: row 04 on opencode 10/10 PASS (/tmp/fx-row04-opencode-combined.log).
Task 04: bench baseline (bench-baseline.json, 3 reps, at 470cf25): caught cli-inline-search 3/3, empty-throws 3/3, missing-returns-empty 3/3, path-no-check 3/3, search-case-sensitive 3/3, search-query-only 3/3, readme-binary 2/3, path-dotdot-only 1/3 (21 of 24); control false positives 1/3.
Combined fixture: combined-a PASS (38m, 22.6M tokens, controller 66 calls, 6.3M, ctx median 92K, growth 6.2K, fixRounds 0), combined-b PASS (45m, 24.0M tokens, controller 59 calls, 5.9M, ctx median 101K, growth 9.9K, fixRounds 0); caught at end 6/6 both; mergeDefects 0 both. Kept transcripts show tasks 04 and 05 ran in parallel in both runs (dispatch lines, merging lines), and task 06's one-sided claim did not run in parallel. The controller wrote each dispatch line twice, once with unexpanded shell variables ($B, $R, %s): deferred minor for task 11's wording.
Medians against baseline: wall 41.5m vs 44m (about 6 percent faster); total tokens 23.3M vs 21.1M (about 11 percent more); controller tokens 6.1M vs 5.1M; controller ctx median about flat. Run-to-run spread (33 to 55 minutes) exceeds the effect; baseline-b had 3 fix rounds, combined runs none.
Combined bench (bench-combined.json) is invalid: lean-b branched at b7d1091, before task 03b's fix rounds 2 and 3, so it ran the old cases (no test files, one-line report); control-2 and control-3 flag the missing test. Controller sequencing error. Merged lean-build into lean-b (f2c1d24, check-all ALL GREEN) and reran the combined bench as three single-rep runs in parallel (labels combined2-r1 to combined2-r3). The combined fixture builds stay valid: the fixture did not change after b7d1091 except task 11's step 4 fix, which lean-b had.
Coverage audit dispatched (opus) against lean-b f2c1d24.
Coverage audit (opus, findings/coverage-audit.md): 0 Critical, 5 Important, 9 Minor. G1: the Plan complete line is written after the last commit, so the base branch never sees it. G2: build-cost misreads builds after tasks 08 and 11 (combined-b reads tasksCompleted 4, fixRounds 0, reviewer 0); totals, wall clock and controller tokens stay sound, per-type and per-task numbers for the combined runs do not. G3 and G12: task 13's ship rule and the step 2 and 3 rules have no owner after Ruling Q; the 3x growth target is missed. G4: the fix-loop hand-off from task 08 is not measured live. G5: in the live combined builds the controller ran the post-rebase tests with `|| true` and never compared file lists: the parallel guards were not followed in practice.
Corrected combined bench (combined2-r1 to r3, at f2c1d24): 23 of 24 against baseline 21 of 24. Control false positives are not usable on either side: all six control reviews flag the control's real test gaps; score.js counts dash bullets only, so the two baseline reviews that used numbered lists read as clean (deferred Minor M4 of task 03b).
Owner decision (2026-09-23): step 3 ships on quality evidence and is verified on the next real long build; build-cost gets fixed for post-08 transcripts (G2) so that comparison works.
Verdicts (measurements.md): step 2 ship; step 3 ship (owner decision); step 4 dropped (about 6 percent wall gain against a 15 percent bar; live controller skipped guards, audit G5). Tasks 10 and 11 reverted on lean-b (61acd3c, 5453607, d399b61); isolated_test_execution unset; task 12 kept. check-all ALL GREEN after the reverts. Ledger consolidated into lean-b, which is the final branch.
Final fixes batched before the final review: G1 (commit the Plan complete line) and G2 (build-cost on post-08 transcripts).
G1 and G2: batched fix DONE (9a7b1a6): fx-implement commits the Plan complete line with the ledger; build-cost reads post-08 ledger writes and dispatch shapes. Controller verified: combined-a and combined-b read tasksCompleted 6, fixRounds 2; 61606dd5 still 709 calls; baseline runs re-scored with the same code read tasksCompleted 6. Growth per task: baseline median 7.3K, combined median 6.1K (measurements.md). Deviation: this batched fix gets no separate task review; the final branch review is pointed at 9a7b1a6 explicitly.
Task 03b: minor (deferred): score.js counts dash bullets only; numbered findings undercount (now shown to bias the control false-positive count).
Final branch review: fx-review branch mode, fixed point 649da01 (merge base with main), head 31214e4, 51 commits, 171 files (109 outside docs/plans). Passes: broad reviewer (opus), spec (opus), standards (sonnet), lens security (credential copy and jail in scratch-home.sh, run-test.sh, run-reps.sh), lens silent-failure (catch and exit-code handling in build-cost, traps, score, rows), unprimed devils-advocate. No database, a11y or pipeline trigger. /code-review is not available in this session; the broad reviewer carries correctness.
Final review: spec (0 C, 2 I, 6 M), security lens (2 I, 4 M), standards (2 hard, 4 judgement), adversarial (3 High, 2 Important, 3 Minor) in findings/final/. Controller check for adversarial 2: 0 of 4 kept build transcripts mention review-bench or hidden/traps.
Final review: silent-failure lens 1 Critical, 3 Important, 1 Minor (findings/final/silent-failure.md). Waiting on the broad reviewer.
Final review: broad (opus) spec warning 2, quality changes requested, 0 C, 2 I, 10 M, ready with fixes (findings/final/broad.md). All six passes in findings/final/.
Ruling T: spec warning G4 (the fix-loop hand-off from task 08 was never measured live) is verified on the next real long build together with step 3 (owner decision); spec warning G7 (opencode subagents still receive the plans block) stays as task 06 accepted, because opencode's plugin does not tell a subagent session apart. Cost if wrong: an unmeasured hand-off defect reaches a real build, caught by that build's first fix round, which the owner is told to watch.
Final fix wave dispatched: ONE fixer (opus; reason: the list spans shell safety, skill text and scorers) with every Critical and Important across the six passes plus the two standards hard violations; Minors deferred except the parallel scaffolding removal the broad reviewer requires. Then exactly one scoped re-review; no second wave.
Owner decision (2026-09-23): skills/fx-implement/SKILL.md is not split or shortened; its length over the fx-authoring 500-line limit is accepted, because moving rules to a reference file risks a controller missing them. Standards hard violation 1 closed as an owner ruling; fix-loop.md only gains a table of contents. Sent to the fix-wave agent.
Final fix wave DONE (c92ec3e..2561492, six commits): A to J fixed; K reduced to the fix-loop.md table of contents per the owner; SKILL.md differs from c92ec3e only in the lens-reply save (Write tool instead of heredoc), 850 lines. check-all ALL GREEN per the fixer. One scoped re-review dispatched (opus).
Final re-review (opus, findings/final/rereview.md): 15 of 16 fixed; open: the bench jail still lets the reviewer read docs/plans/2026-09-23-lean-build (task 03b lists every case, defect and match words), the runs JSON and tests/review-bench/README.md; new Important: tests/conformance/jail-probe.test.sh:81 asserts a hidden .git is size zero, which fails where .git is a directory (tmpfs directory size 40), so check-all goes red on a normal checkout. Controller reproduced it in a fresh clone: FAIL jail_hide: a hidden .git reads as nothing.
Ruling U: both residuals are load-bearing (a red gate on main after merge; bench answer keys readable), so neither is parked. One minimal fixer is dispatched for exactly these two items; the controller verifies by running check-all and the probe in a fresh clone instead of a second review. Deviation from "no second fix wave", reported to the owner. The five new Minors from the re-review are deferred.
Ruling U fix DONE (62aa03e): jail-probe asserts a hidden .git as empty for both file and directory shapes; the bench row hides docs/plans and tests/review-bench/README.md; row-safety.test.sh asserts them.
